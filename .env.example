# Optional: LLM API key for enhanced text generation
# Not required - template-based generation works without it
# OPENAI_API_KEY=sk-...

# Database output path (optional, defaults to output/asana_simulation.sqlite)
# DATABASE_PATH=output/asana_simulation.sqlite

# Random seed for reproducibility (optional)
# RANDOM_SEED=42
